##
```
git pull // 처음 했을 때 or git 에서 변경!!!! 했을 때
git clone 주소
git status //  상태확인
git add . // 추가하기
git status //  상태확인
git commit -m "-------"  // commit 메시지
git push origin main // main 이름으로 push 함 
```
